- when we do `netstat -tuplen` (for instance in mariadb) customise the `Foreign Address` field to point to the container supposed to reach the service (instead of 0.0.0.0.:*)
- handle correctly users and permissions (www-data, nginx, trobin)
- change wordpress admin username and password
- check if container restart is working as supposed to
